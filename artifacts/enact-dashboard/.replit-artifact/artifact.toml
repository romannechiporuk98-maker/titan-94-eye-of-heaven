kind = "web"
previewPath = "/"
title = "ENACT Dashboard"
version = "1.0.0"
id = "artifacts/enact-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20474

[services.development]
run = "pnpm --filter @workspace/enact-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/enact-dashboard", "run", "build" ]
publicDir = "artifacts/enact-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20474"
BASE_PATH = "/"
